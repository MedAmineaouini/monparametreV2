{% extends 'base.html.twig' %}

{% block title %}Affreteur{% endblock %}

{% block body %}
    <h1>Affreteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Seqaffret</th>
                <td>{{ affreteur.seqaffret }}</td>
            </tr>
            <tr>
                <th>Codaffret</th>
                <td>{{ affreteur.codaffret }}</td>
            </tr>
            <tr>
                <th>Libaffret</th>
                <td>{{ affreteur.libaffret }}</td>
            </tr>
            <tr>
                <th>Compagnie</th>
                <td>{{ affreteur.compagnie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_affreteur_index') }}">back to list</a>

    <a href="{{ path('app_affreteur_edit', {'seqaffret': affreteur.seqaffret}) }}">edit</a>

    {{ include('affreteur/_delete_form.html.twig') }}
{% endblock %}
