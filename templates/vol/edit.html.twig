{% extends 'base.html.twig' %}

{% block title %}RFT | Modification Vol{% endblock %}

{% block body %}
<div class="card shadow rounded-3 border-0">
    <div class="card-header text-white d-flex justify-content-between align-items-center">
        <h5 class="mb-0">📋 Vol  | Modification </h5>
    </div>
    <hr>
    <div class="card-body">

    {{ include('vol/_form.html.twig', {'button_label': 'Update'}) }}

        /div>
    </div>
    {#    <a href="{{ path('app_reseau_index') }}">back to list</a>#}

    {#    {{ include('reseau/_delete_form.html.twig') }}#}
{% endblock %}