{% extends 'base.html.twig' %}

{% block title %}GroupementClient{% endblock %}

{% block body %}
    <h1>GroupementClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Seqgroupementclient</th>
                <td>{{ groupement_client.seqgroupementclient }}</td>
            </tr>
            <tr>
                <th>Groupeclient</th>
                <td>{{ groupement_client.groupeclient }}</td>
            </tr>
            <tr>
                <th>Nomgroupeclient</th>
                <td>{{ groupement_client.nomgroupeclient }}</td>
            </tr>
            <tr>
                <th>Archiver</th>
                <td>{{ groupement_client.archiver }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_groupement_client_index') }}">back to list</a>

    <a href="{{ path('app_groupement_client_edit', {'seqgroupementclient': groupement_client.seqgroupementclient}) }}">edit</a>

    {{ include('groupement_client/_delete_form.html.twig') }}
{% endblock %}
