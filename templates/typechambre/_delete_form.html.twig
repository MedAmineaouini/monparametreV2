{#<form method="post" action="{{ path('app_typechambre_delete', {'seqtypechambre': typechambre.seqtypechambre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">#}
{#    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typechambre.seqtypechambre) }}">#}
{#    <button class="btn">Delete</button>#}
{#</form>#}
<form method="post"
      action="{{ path('app_typechambre_delete', {'seqtypechambre': typechambre.seqtypechambre}) }}"
      class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typechambre.seqtypechambre) }}">
    <button type="button"
            class="dropdown-item text-danger border-0 bg-transparent w-100 text-start delete-btn"
            data-entity-type="ce pays"
            data-confirm-message="Êtes-vous sûr de vouloir supprimer ce pays ? Cette action est irréversible."
            data-success-message="Le pays a été supprimé avec succès">
        <i class="icon-base bx bx-trash me-1"></i> Supprimer
    </button>
</form>