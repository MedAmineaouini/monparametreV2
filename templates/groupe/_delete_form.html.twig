<form method="post" action="{{ path('app_groupe_delete', {'seqgroupe': groupe.seqgroupe}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupe.seqgroupe) }}">
    <button class="btn">Delete</button>
</form>
