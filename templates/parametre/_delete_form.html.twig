<form method="post" action="{{ path('app_parametre_delete', {'seqparam': parametre.seqparam}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parametre.seqparam) }}">
    <button class="btn">Delete</button>
</form>
