
    {{ form_start(form) }}

    <!-- 🔹 Ligne 1 -->
    <div class="form-group row mb-3">
        <label class="col-sm-2 control-label">Entête *</label>
        <div class="col-sm-4">
            {{ form_widget(form.entete1, { attr: { class: 'form-control' } }) }}
        </div>

        <label class="col-sm-2 control-label">Adresse</label>
        <div class="col-sm-4">
            {{ form_widget(form.adresse, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Ligne 2 -->
    <div class="form-group row mb-3">
        <label class="col-sm-2 control-label">Email</label>
        <div class="col-sm-4">
            {{ form_widget(form.email, { attr: { class: 'form-control' } }) }}
        </div>

  <label class="col-sm-2 control-label">Code Postal</label>
        <div class="col-sm-1">
            {{ form_widget(form.cp, { attr: { class: 'form-control' } }) }}
        </div>

        <label class="col-sm-1 control-label">Ville</label>
        <div class="col-sm-2">
            {{ form_widget(form.ville, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Ligne 3 -->
    <div class="form-group row mb-3">
        <label class="col-sm-2 control-label">Tél</label>
        <div class="col-sm-4">
            {{ form_widget(form.tel, { attr: { class: 'form-control' } }) }}
        </div>

        <label class="col-sm-2 control-label">Fax</label>
        <div class="col-sm-4">
            {{ form_widget(form.fax, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Ligne 4 -->
    <div class="form-group row mb-3">
        <label class="col-sm-2 control-label">Seq resa</label>
        <div class="col-sm-4">
            {{ form_widget(form.seqresa, { attr: { class: 'form-control' } }) }}
        </div>

        <label class="col-sm-2 control-label">Contact Fr</label>
        <div class="col-sm-4">
            {{ form_widget(form.contactfr, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Ligne 5 -->
    <div class="form-group row mb-3">
        <label class="col-sm-2 control-label">Entête (1)</label>
        <div class="col-sm-4">
            {{ form_widget(form.entete2, { attr: { class: 'form-control' } }) }}
        </div>

        <label class="col-sm-2 control-label">Contact </label>
        <div class="col-sm-4">
            {{ form_widget(form.contact, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Ligne 6 -->
    <div class="form-group row mb-3">
        <label class="col-sm-2 control-label">Euro</label>
        <div class="col-sm-4">
            {{ form_widget(form.euro, { attr: { class: 'form-control' } }) }}
        </div>

        <label class="col-sm-2 control-label">Expediteur</label>
        <div class="col-sm-4">
            {{ form_widget(form.expediteur, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Ligne 7 -->
    <div class="form-group row mb-3">
        <div class="col-sm-3">
               <div class="form-check form-switch">
                    {{ form_widget(form.alerte, {'attr': {'class': 'form-check-input', 'role': 'switch'}}) }}
                    {{ form_label(form.alerte, 'Alerte', {'label_attr': {'class': 'form-check-label fw-semibold'}}) }}
                </div>
        </div>
    </div>

    <!-- 🔹 Ligne 8 -->
    <div class="form-group row mb-3">
        <label class="col-sm-3 control-label">Ligne à imprimer si code client ≠ 12300 et 11689</label>
        <div class="col-sm-3">
            {{ form_widget(form.btobvol, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Ligne 9 -->
    <div class="form-group row mb-3">
        <label class="col-sm-3 control-label">Ligne à imprimer Groupe</label>
        <div class="col-sm-3">
            {{ form_widget(form.pays, { attr: { class: 'form-control' } }) }}
        </div>
    </div>

    <!-- 🔹 Submit -->
    <div class="form-group row justify-content-between">
        <div class="col-auto">
            <a href="{{ path('app_utilisateur_index') }}" class="btn btn-secondary">Annuler</a>
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-primary">Enregistrer</button>
        </div>
    </div>

    {{ form_end(form) }}
