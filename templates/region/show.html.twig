{% extends 'base.html.twig' %}

{% block title %}Region{% endblock %}

{% block body %}
    <h1>Region</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Seqregion</th>
                <td>{{ region.seqregion }}</td>
            </tr>
            <tr>
                <th>Libregion</th>
                <td>{{ region.libregion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_region_index') }}">back to list</a>

    <a href="{{ path('app_region_edit', {'seqregion': region.seqregion}) }}">edit</a>

    {{ include('region/_delete_form.html.twig') }}
{% endblock %}
