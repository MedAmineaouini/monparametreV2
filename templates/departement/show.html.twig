{% extends 'base.html.twig' %}

{% block title %}Departement{% endblock %}

{% block body %}
    <h1>Departement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Seqdepartement</th>
                <td>{{ departement.seqdepartement }}</td>
            </tr>
            <tr>
                <th>Code_departement</th>
                <td>{{ departement.codeDepartement }}</td>
            </tr>
            <tr>
                <th>Lib_departement</th>
                <td>{{ departement.libDepartement }}</td>
            </tr>
            <tr>
                <th>Codecommercial</th>
                <td>{{ departement.codecommercial }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_departement_index') }}">back to list</a>

    <a href="{{ path('app_departement_edit', {'seqdepartement': departement.seqdepartement}) }}">edit</a>

    {{ include('departement/_delete_form.html.twig') }}
{% endblock %}
